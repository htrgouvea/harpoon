package App::Helpers;

use strict;
use warnings;

sub new {
    my ($self, $app) = @_;

    # $app -> helper ( AppHelpers =>
    #     sub { 
    #         return 'I am your helper and I live in a plugin!';
    #     }
    # );

    # return 1;
}

1;